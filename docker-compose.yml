version: '3'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

  frontend:
    build:
      context: .
      dockerfile: front/Dockerfile
    ports:
      - "3000:80"
